# Optional dependencies for live API connectivity and interactive formatting
# Core agent executes with 0 external dependencies (Standard Library Python 3.10+)

google-genai>=0.1.1; extra == "live"
pydantic>=2.0.0; extra == "dev"
pytest>=7.0.0; extra == "dev"
